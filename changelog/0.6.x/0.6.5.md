---
summary: README "Let the agent drive" scaffolding pitch, package description tightened, dependency sweep (ext-apps 1.7, vitest 4.1.5, workers-types)
breaking: false
---

# 0.6.5 — 2026-04-22

Documentation + dependency release. The README now highlights the `bunx @cyanheads/mcp-ts-core init` workflow and the Agent Skills shipped with the scaffold as the intended entry point for agent-assisted MCP server development. Package description tightened to match. No framework behavior changes.

## Changed

- **`README.md`** — added "Let the agent drive" section positioning `bunx @cyanheads/mcp-ts-core init <name>` as the primary onboarding path and framing the included Agent Skills (`setup` → `design-mcp-server` → scaffolding → testing → release) as the coding agent's build cycle.
- **`package.json`** — tightened description: dropped the "Build tools, not infrastructure" tagline and condensed runtime support to "Bun/Node/Cloudflare Workers" to match README copy and npm card.
- **`.gitignore`** — added `claude-plans/` to the local agent-scratchpad ignore list alongside `.agents/`, `announcements/`, `agent-feedback/`.

## Dependencies

- `@modelcontextprotocol/ext-apps` → `^1.7.0` (minor)
- `@cloudflare/workers-types` → `^4.20260422.1`
- `vitest`, `@vitest/ui`, `@vitest/coverage-istanbul` → `4.1.5`
