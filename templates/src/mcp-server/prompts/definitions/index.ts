/**
 * @fileoverview Prompt definitions barrel export.
 * @module mcp-server/prompts/definitions
 */

import type { PromptDefinition } from '@cyanheads/mcp-ts-core/prompts';

// Import prompt definitions here:
// import { myPrompt } from './my-prompt.prompt.js';

export const allPromptDefinitions: PromptDefinition[] = [
  // Register prompts here:
  // myPrompt,
];
