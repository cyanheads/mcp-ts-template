/**
 * @fileoverview Resource definitions barrel export.
 * @module mcp-server/resources/definitions
 */

import type { AnyResourceDefinition } from '@cyanheads/mcp-ts-core/resources';

// Import resource definitions here:
// import { myResource } from './my-resource.resource.js';

export const allResourceDefinitions: AnyResourceDefinition[] = [
  // Register resources here:
  // myResource,
];
